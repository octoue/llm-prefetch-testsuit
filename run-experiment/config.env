# ============================================
# Prefetch A/B 实验配置文件（唯一参数来源）
# ============================================
# 所有参数仅在此文件定义，脚本不再有写死的默认值
# 修改此文件即可调整全部行为

# ---------- vLLM 启动 (start_vllm.sh) ----------
MODEL_PATH=/lpai/models/Qwen__Qwen3-8B/25-07-26-0349
GPU_MEMORY_UTILIZATION=0.5
NUM_GPU_BLOCKS_OVERRIDE=400
KV_OFFLOADING_SIZE=5
SWAP_SPACE=256
API_PORT=8000
VLLM_LOG=vllm_state.log
VLLM_HOST=0.0.0.0
VLLM_MAX_NUM_SEQS=256
VLLM_BLOCK_SIZE=16
VLLM_TENSOR_PARALLEL_SIZE=1

# ---------- 测试通用 ----------
API_BASE=http://localhost:8000/v1
API_HOST=localhost:8000
SEED=42

# ---------- run_ab_test.sh ----------
QPS=2.0
NUM_CONV=50
TRACE=data/qwen_traceA_blksz_16.jsonl
TIMEOUT=

# ---------- run_lite_test.sh ----------
LITE_QPS=2.0
LITE_NUM_CONV=18
LITE_TRACE=data/lite_dataset.jsonl
LITE_FULL_TRACE=data/qwen_traceA_blksz_16.jsonl
LITE_TIMEOUT=300
REQUEST_TIMEOUT=180
PREFETCH_LEAD_TIME=3.0
TB_PORT=6007

# ---------- PCIe Profiling (start_vllm_pcie.sh, run_lite_test_pcie.sh) ----------
# vLLM 源码路径，用于 setup_pcie_env.sh 和 visualize_pcie_gantt.py（相对 run-experiment 目录，code/vllm 与 code/llm-prefetch-testsuit 同级）
VLLM_SRC=../../vllm16
# Profiler 输出目录（trace.json、pcie_events_*.json）
PCIE_PROFILER_DIR=./profiler_output
# 多卡 PP：Pipeline Parallelism 大小（2 = 2 张卡，用于观察 P2P over PCIe）
VLLM_PIPELINE_PARALLEL_SIZE=2
# heavy-lite 工作负载（更容易打出 PCIe 竞争）
LITE_PCIE_TRACE=data/heavy_lite_dataset.jsonl
LITE_PCIE_SAMPLING_MODE=heavy
LITE_PCIE_MAX_INPUT_LENGTH=5000
LITE_PCIE_TIER_SHORT=2
LITE_PCIE_TIER_MEDIUM=8
LITE_PCIE_TIER_LONG=14
LITE_PCIE_NUM_CONV=24
LITE_PCIE_SCHEDULE_MODE=scaled-timestamp
LITE_PCIE_PREFETCH_LEAD_TIME=1.0
